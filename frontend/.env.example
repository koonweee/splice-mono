# Optional explicit browser-facing API origin for frontend requests with credentials.
# If unset, the app derives an API hostname by adding `-api` to the first host label.
VITE_API_BASE_URL=http://localhost:3000
