These captures are superseded because the original SQL observer retained records across unmeasured warmups. This caused the 1m-row legacy-search run to exceed Node’s 4 GiB heap; it is a harness failure, not evidence of application OOM. The corrected observer records only measured intervals, releases records between calls, and counts row bytes without constructing a giant JSON string. The main matrix is rerun with observer v2 for both variants. Original source/fixtures/timings remain here for audit only.
